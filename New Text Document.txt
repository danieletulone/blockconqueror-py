music = pygame.mixer.music.load('assets/background/dark-soundscape.mp3')
            pygame.mixer.music.play(-1)

if intero_gioco > fine_flip[flip] + 2:
        flip += 1
        random_block = False

    if intero_gioco > inizio_flip[flip] + 2 and intero_gioco < fine_flip[flip] + 2:
        screen.blit(pygame.transform.flip(screen, False, True), (0, 0))
        flipped = True
    else:
        flipped = False